require_relative '../src/in'

describe "in" do
  context "#download_binary" do
    it "downloads binary arrording to given version" do

    end

    it "raises error if download fails" do

    end
  end
end